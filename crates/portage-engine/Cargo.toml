[package]
name = "portage-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Index, policy, planner, and executor for Portage (stub until PR 4+)"

[dependencies]
portage-core = { path = "../portage-core" }
