# Starter agent skills

Same pack as `C:\Repos\Scripts\.agents\skills`. Keep them in sync when you add or edit a skill there.

Machine-wide install (every repo, every tool):

```powershell
C:\Repos\Scripts\Install-AgentSkills.ps1
```
